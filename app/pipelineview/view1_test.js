'use strict';

describe('dashboard.pipeline module', function() {

  beforeEach(module('dashboard.pipeline'));

  describe('pipeline controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('PipelineView1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});